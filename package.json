{
  "name": "testapp",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "cd ./render && npm run serve & wait-on http://localhost:8080 && npm run serve",
    "prestart": "echo start",
    "serve": "nodemon --watch ./index.js --exec electron ."
  },
  "author": "",
  "license": "ISC",
  "dependencies": {},
  "devDependencies": {
    "electron": "^8.2.1",
    "nodemon": "^2.0.3",
    "wait-on": "^4.0.2"
  }
}
